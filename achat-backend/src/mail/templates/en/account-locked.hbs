{{#> layout title="Account Access Restricted"}}
    <h2>Account Access Restricted</h2>
    
    <p>Hello {{userName}},</p>
    
    <div class="message-box">
        <p class="text-large">⚠️ Account Status: Locked</p>
        <p>Your account has been locked due to:</p>
        <p><span class="highlight">{{lockReason}}</span></p>
        <p>Lock Date: {{lockDate}}</p>
    </div>

    <div class="message-box">
        <p class="text-large">Impact of Account Lock:</p>
        <ul>
            <li>Unable to submit new bids</li>
            <li>Limited access to platform features</li>
            <li>Cannot participate in new purchase requests</li>
            <li>Existing bids remain valid but cannot be modified</li>
        </ul>
    </div>

    <p>To restore full access to your account, you need to:</p>
    
    {{#if isCompanyRequired}}
    <div class="message-box">
        <p class="text-large">Required Actions:</p>
        <p>Either join an existing company or register a new one:</p>
        <div style="text-align: center; margin: 15px 0;">
            <a href="{{joinCompanyUrl}}" class="button">Join Company</a>
            <p style="margin: 15px 0;">or</p>
            <a href="{{registerCompanyUrl}}" class="button">Register Company</a>
        </div>
    </div>
    {{/if}}

    {{#if hasCustomActions}}
    <div class="message-box">
        <p class="text-large">Steps to Unlock:</p>
        <ul>
            {{#each unlockSteps}}
            <li>{{this}}</li>
            {{/each}}
        </ul>
    </div>
    {{/if}}
    
    <div class="message-box">
        <p class="text-large">Need Assistance?</p>
        <p>If you need help resolving this issue, please contact our support team:</p>
        <ul>
            <li>Email: {{supportEmail}}</li>
            <li>Phone: {{supportPhone}}</li>
            <li>Support Hours: {{supportHours}}</li>
        </ul>
    </div>
    
    <p class="text-small">Reference ID: {{lockReferenceId}} - Please include this ID in all correspondence about this issue.</p>
{{/layout}} 